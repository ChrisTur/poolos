[build]
  command = "npx prisma generate && ([ -z \"$DATABASE_URL\" ] && echo 'Skipping migrate: DATABASE_URL not set' || npx prisma migrate deploy) && npm run build"
  publish = ".next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
